# 生产环境依赖 (Production Dependencies)
# 安装方法: pip install -r requirements-prod.txt

# 首先安装核心依赖
-r requirements.txt

# =============================================================================
# 生产环境Web服务器 (Production Web Server)
# =============================================================================
gunicorn==21.2.0                # WSGI HTTP服务器
gevent==23.9.1                  # 异步网络库

# =============================================================================
# 性能监控 (Performance Monitoring)
# =============================================================================
psutil==5.9.6                   # 系统资源监控

# =============================================================================
# 日志处理 (Logging)
# =============================================================================
# loguru==0.7.2                 # 增强的日志库（可选）

# =============================================================================
# 安全增强 (Security Enhancements)
# =============================================================================
# cryptography==41.0.7          # 加密库（可选）

# =============================================================================
# 数据库连接池 (Database Connection Pooling)
# =============================================================================
# SQLAlchemy==2.0.23            # ORM和连接池（可选）

# =============================================================================
# 缓存 (Caching)
# =============================================================================
# redis==5.0.1                  # Redis缓存（可选）
# Flask-Caching==2.1.0          # Flask缓存扩展（可选）

# =============================================================================
# 部署说明 (Deployment Notes)
# =============================================================================
# 生产环境建议使用以下启动命令：
# gunicorn --bind 0.0.0.0:24377 --workers 4 --worker-class gevent app:app
# 
# 或者使用配置文件启动：
# gunicorn --config gunicorn.conf.py app:app 